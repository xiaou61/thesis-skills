interface:
  display_name: "论文标准化"
  short_description: "基于真实程序生成本科系统类论文"
  default_prompt: "Use $thesis-standardizer to generate an undergraduate system-design thesis from a real program. First extract school template rules if a .docx template exists, then build project evidence from the source code, fill standard-profile.yaml, thesis-ai-spec.yaml, and figure-registry.yaml, draft chapters 1-6 from evidence, generate only the required Visio use-case/function-architecture/ER diagrams and database or clearly labeled data-object three-line tables, keep editable sources registered, and list missing evidence instead of inventing facts."
policy:
  allow_implicit_invocation: true
